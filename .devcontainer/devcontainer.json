{
    "name": "C++ with Google Test",
    "image": "mcr.microsoft.com/devcontainers/cpp:1-ubuntu22.04",
    "postCreateCommand": "apt-get update && apt-get install -y libgtest-dev && cd /usr/src/gtest && cmake CMakeLists.txt && make && cp lib/*.a /usr/lib",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-vscode.cmake-tools"
            ]
        }
    }
}